node_modules/
.next/
.env
.env.local
*.log
dist/
build/
.turbo/
Claude-Production-Grade-Suite/.orchestrator/receipts/
.claude/worktrees/
coverage/
playwright-report/
test-results/
.eslintcache

# Test run artifacts (QA-owned tests/ tree — see tests/vitest.config.ts, tests/e2e/playwright.config.ts)
tests/test-results/
tests/playwright-report/
tests/coverage/html/
tests/coverage/*.xml
playwright-report/
test-results/


# Scripts/logs manuais de depuração local (gerados durante sessões de suporte no Windows —
# ver .bat na Área de Trabalho) — nunca fazem parte do código-fonte, mas ficam na raiz do repo
# porque é onde os scripts .bat precisam rodar (cd para a pasta do projeto). Nunca commitar.
/.data/
/*_log.txt
/atualizar_planos.sql
