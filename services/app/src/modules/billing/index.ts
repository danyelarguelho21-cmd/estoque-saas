// Módulo: billing — planos, assinatura, faturas, webhook do PagBank.
// Depende exclusivamente de PaymentProvider (@estoque-saas/shared) — nunca do SDK do
// PagBank diretamente (ADR-004). Candidato a extração futura para serviço próprio (ADR-001).
// Implementação: fase BUILD. Ver api/openapi/billing.yaml.
export {};
